# Setup is handled by setup.bat (it pulls torch from PyTorch's CUDA 11.8 index).
# If you want to install manually instead:
#     pip install torch==2.0.1+cu118 torchvision==0.15.2+cu118 \
#         --index-url https://download.pytorch.org/whl/cu118
#     pip install -r requirements.txt

# numpy must stay on 1.x — torch 2.0.1+cu118 was built against numpy 1.x;
# pulling numpy>=2 breaks runtime with "_ARRAY_API not found".
numpy>=1.23,<2

ultralytics>=8.0
supervision>=0.28

# ONNX runtime — needed by `--lite` on CPU: count_inout_head.py exports the
# nano model to ONNX for ~2x faster CPU inference. Without these, Ultralytics
# silently auto-installs them on first run (the "requirements not found,
# attempting AutoUpdate" message).
onnx>=1.14
onnxruntime>=1.15

# REST notifications on each crossing (--api-url). The gender model uses
# OpenCV's bundled DNN module, so it needs no extra package here.
requests>=2.28
